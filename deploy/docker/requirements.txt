# MotorPM v2.0 运行时依赖(基于本机验证版本)
# 说明:
#   - easyocr 未包含:体积巨大(torch ~2GB),容器内默认不装;
#     需要 OCR 功能时取消下方注释重新构建
#   - 首次构建会从国内镜像源下载,耗时约 2-5 分钟

fastapi==0.115.6
uvicorn==0.34.0
websockets==13.1
sqlalchemy==2.0.36
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.19
aiofiles==24.1.0
aiohttp>=3.9
python-jose==3.3.0
openpyxl==3.1.5
python-docx==1.1.2
python-pptx==1.0.2
requests>=2.31
qrcode>=7.4
pandas>=2.0
xlrd>=2.0
Pillow>=10.0
PyMuPDF>=1.24
chardet>=5.0

# ── 可选:OCR 识别(需要时取消注释,镜像体积增加 ~2GB)──
# easyocr>=1.7
